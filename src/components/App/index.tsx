import { Form, Table } from "components";
import { OutsideAppContainer } from "components";

export const App = () => {
  return (
    <OutsideAppContainer>
      <Form />
      <Table />
    </OutsideAppContainer>
  );
};
